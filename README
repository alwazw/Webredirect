The index.html will load, collect the fingerprint data, send it to the Flask server, and then redirect to the url you want (here ex: Google.com). 
The collected data will be logged to fingerprints.log.
After starting the Flask app, navigate to http://localhost:8080 or (Server-IP:8080) in your browser. 
Verify fingerprint data was collected in fingerprints.log.

0. Pre-requisits
Python3-pip
sudo apt install python3-pip

1. Clone Repo

-- Structure

/home/alwazw/test_dir
├── app.py
├── fingerprints.log
├── requirements.txt
├── static
│   └── js
│       └── fingerprint.js
└── templates
    └── index.html

2. Create Folders
cd
mkdir webredirect
mkdir -p "$BASE_DIR/static/js"
mkdir -p "$BASE_DIR/templates"

3. Create the files
touch "$BASE_DIR/app.py"
touch "$BASE_DIR/fingerprints.log"
touch "$BASE_DIR/requirements.txt"
touch "$BASE_DIR/static/js/fingerprint.js"
touch "$BASE_DIR/templates/index.html"

4. Install Dependencies:
sudo apt install python3
pip install -r requirements.txt

5. Run Flask App:
python app.py

